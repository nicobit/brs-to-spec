# F-003.1 — Score pipeline - happy path

## Metadata

| Field | Value |
|---|---|
| Story ID | F-003.1 |
| Epic | E-002 — AI Scoring & Risk Decisions |
| Actor | Scoring Service |
| Layers | Backend / Integration |
| Priority | Must |
| Increment | D1 |
| Status | Draft |

---

## User Story

As a **scoring service**, I produce a score and publish `score.generated` so downstream consumers can act.

---

## Business Context

Automating scoring reduces manual work for underwriters and standardises risk decisions. Deterministic scoring with provenance is required for regulator audits.

---

## Linked Requirements

- REQ-004, REQ-017, FR-006

---

---

## Implementation Guidance

- Call Experian adapter as part of scoring when required; include model_version and explainability metadata in the event.

---

## Acceptance Criteria

### AC-001 — Scoring returns `[integration]`

```gherkin
Scenario: Score produced for valid ARN
  Given a valid application arn
  When scoring service is invoked
  Then it returns a numeric score, model_version and emits score.generated
```

### AC-002 — Performance `[non-functional]`

```gherkin
Scenario: Scoring completes within SLA
  Given high load under 95th percentile
  When scoring runs
  Then response latency is under 2 seconds
```

---

## Test Expectations

| Test Type | What to verify | Why |
|---|---|---|
| unit | scoring logic and model version handling | correctness and determinism |
| integration | end-to-end scoring including Experian call | ensures external data integration |

