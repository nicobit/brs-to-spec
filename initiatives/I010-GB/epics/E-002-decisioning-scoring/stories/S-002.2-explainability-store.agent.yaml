story_id: S-002.2
title: "Store explainability artifacts"
story_type: "backend-endpoint"
layer: "backend"

requirements_implemented:
  - NFR-007

requirements_referenced:
  - NFR-003

depends_on:
  - story_id: S-002.1
    reason: "Needs normalized scoring.job events to receive explainability payloads"
    consumed_contract_summary: "application.scoring.job event schema"

in_scope:
  - "Persist explainability artifacts and provide retrieval API"

out_of_scope:
  - "Real-time streaming analytics on explainability artifacts"

implementation_contract:
  touched_components:
    - "Explainability service"
  operations:
    - type: "api"
      target: "POST /api/explainability"
      expected_result: "artifact persisted and immutable"

required_tests:
  integration:
    - "Persistence and retrieval of explainability artifacts"

  api:
    - "POST /api/explainability -> persists artifact"

done_evidence:
  - "Artifacts stored and sample retrieval responses recorded"
