story_id: S-003.2
title: "Underwriter case API"
story_type: "backend-endpoint"
layer: "backend"

requirements_implemented:
  - FR-019

requirements_referenced: []

depends_on:
  - story_id: S-003.1
    reason: "Needs normalized AML screening results to create cases"
    consumed_contract_summary: "aml screening normalized result"

in_scope:
  - "Create and retrieve compliance cases via API"

out_of_scope:
  - "Underwriter UI beyond list and detail views"

implementation_contract:
  touched_components:
    - "Case API"
  operations:
    - type: "api"
      target: "POST /api/cases"
      expected_result: "case persisted and retrievable"

required_tests:
  integration:
    - "Case creation and retrieval"

  api:
    - "POST /api/cases -> creates case and returns id"

done_evidence:
  - "API contract and sample calls"
