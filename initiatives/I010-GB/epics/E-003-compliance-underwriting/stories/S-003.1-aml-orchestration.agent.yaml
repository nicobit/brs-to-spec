story_id: S-003.1
title: "AML orchestration connector"
story_type: "event-consumer"
layer: "backend"

requirements_implemented:
  - FR-012

requirements_referenced: []

depends_on: []

in_scope:
  - "Invoke AML provider and return normalized screening results"

implementation_contract:
  touched_components:
    - "AML connector"
  operations:
    - type: "integration"
      target: "invoke-provider -> normalized-result"
      expected_result: "structured screening result"

required_tests:
  integration:
    - "Provider invocation and result normalization"

done_evidence:
  - "Connector integration tests"
out_of_scope:
  - "Provider-specific enrichment beyond normalization"
