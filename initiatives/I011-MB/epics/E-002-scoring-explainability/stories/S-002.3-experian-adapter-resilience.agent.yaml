story_id: S-002.3
title: "Experian adapter and resilience"
story_type: "generic"
layer: "integration"

requirements_implemented:
  - FR-009

requirements_referenced:
  - NFR-007

depends_on:
  - story_id: external/experian-contract
    reason: must implement mapping and sample responses
    consumes_contract:
      type: "external"
      summary: "Experian CreditExpert sample responses and mapping"

blocks: []

in_scope:
  - "Adapter with retry, timeout, and circuit breaker"

out_of_scope:
  - "Vendor onboarding tasks"

implementation_contract:
  touched_components:
    - "Experian Adapter"
  touched_files_or_areas:
    - "services/adapters/experian/"
  data_inputs:
    - name: "request_payload"
      type: "object"
      required: true
      validation: "schema per external contract"
  operations:
    - type: "api call"
      target: "Experian CreditExpert API"
      expected_result: "mapped credit report within timeout"
  success_behavior:
    - "Return mapped credit report to scoring service"
  error_handling:
    - "On repeated failure, return explicit error for scoring to fallback"

required_tests:
  unit:
    - "mapping tests"
  integration:
    - "retry and circuit-breaker tests"
  api:
    - "external contract conformance"
  e2e:
    - "scoring integration with adapter stub"
  accessibility:
    - "Not applicable"

done_evidence:
  - "Adapter integration test reports"
