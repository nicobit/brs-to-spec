story_id: S-002.6
title: "UI Binding: Surface score in Underwriter UI"
story_type: "frontend-page"
layer: "frontend"

requirements_implemented:
  - FR-030

requirements_referenced:
  - FR-007

depends_on:
  - story_id: S-002.5
    reason: reads score and explainability payload
    consumes_contract:
      type: "api"
      summary: "GET /api/v1/scores/{id}"

blocks: []

in_scope:
  - "Add UI bindings and example components to render score and explainability payload"

out_of_scope:
  - "Full underwriter workflow replacement"

implementation_contract:
  touched_components:
    - "Underwriter UI"
  touched_files_or_areas:
    - "apps/underwriter/src/components/"
  data_inputs:
    - name: "score_response"
      type: "object"
      required: true
  operations:
    - type: "ui"
      target: "Underwriter Application Detail page"
      expected_result: "Score and explainability are rendered"

required_tests:
  unit:
    - "component rendering tests"
  integration:
    - "fetch and render from API"

done_evidence:
  - "Component demo and snapshot tests"
