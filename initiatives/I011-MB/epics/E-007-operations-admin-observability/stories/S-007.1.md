# S-007.1 — Metrics ingestion & storage

| Story ID | S-007.1 |
|---|---|
| Layers | Backend / Backend API / Infrastructure |

Implement metrics ingestion endpoint, long-term storage in MetricsStore, and retention policy.

| Story Type | Feature |

## User Story

As an operator, I want metrics to be ingested and stored so that dashboards and alerts can be computed.

## Implementation Guidance

Follow contract `implementation-contract.md` for metrics ingestion schema.

## Test Expectations

- Unit tests for ingestion mapping
- Integration tests for storage and retention

## Required Tests

- 2 unit tests
- 1 integration test

```gherkin
Scenario: Ingest metric
	Given a valid metric payload
	When posted to /api/v1/metrics
	Then the metric is accepted and stored
```
