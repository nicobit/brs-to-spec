# API Contract

## Public APIs

- `POST /applications` — create application (traces FR-001)
- `GET /applications/{id}` — fetch application status

## Authentication

- Azure AD bearer tokens; RBAC enforced per AR-010

## Versioning

- v1 initial release
