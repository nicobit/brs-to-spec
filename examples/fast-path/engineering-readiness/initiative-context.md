---
> Generated by: .brs2spec/4-engineering-readiness/02-generate-initiative-context.md
> Consumer: all downstream prompts — implementation, review, handoff
> Update this artifact if the queue schema verification (A01) reveals a schema difference.
---

# Initiative Context

## Metadata

| Field | Value |
|---|---|
| Initiative ID | fast-path-example |
| Initiative slug | password-reset-async |
| Active deliverable | D1 Password Reset Async |
| Context version | 1.0 |
| Generated date | 2026-06-09 |
| Generated from | `engineering-readiness/readiness-check.md` |

## Technology Constraints

| Constraint | Value | Source |
|---|---|---|
| Language / runtime | Same as `auth-service` (check repo) | Existing codebase |
| Background queue | Redis-backed `job-queue` service — already in use for notification emails | input-package.md |
| Email delivery | `email-service` internal HTTP API — do not call directly from handler | input-package.md |
| Deployment target | Existing `auth-service` deployment — no new service | input-package.md |

## Affected Components

| Component | Role | Impact type | Sensitivity |
|---|---|---|---|
| `auth-service` → `POST /auth/password-reset` | Handler being refactored | Modified | Medium — customer-facing |
| `job-queue` | Receives background job | Consumed (existing) | Low — pattern already established |
| `email-service` | Sends password reset email | Consumed (existing, via queue) | Low |

## Architecture Rules in Force

| Rule ID | Rule | Applies to | Non-negotiable? |
|---|---|---|---|
| R-01 | Do not call `email-service` synchronously from an HTTP handler | `auth-service` handler | Yes |
| R-02 | Use existing `job-queue` schema for email jobs — do not introduce a new schema | Queue message | Yes — confirm via A01 before coding |
| R-03 | Reset token logic must not change — only the delivery mechanism | `auth-service` | Yes |

## Governed Boundaries

| Boundary ID | Type | Producer | Consumer(s) | Gate triggered | Gate artifact |
|---|---|---|---|---|---|
| B01 | Event (job-queue) | auth-service | email-service | No — existing schema reused | None |

## Active Quality Gates

No gates triggered for this deliverable.

## Rollback and Regression Sensitivity

| Area | Sensitivity | Reason | Mitigation |
|---|---|---|---|
| Rollback | Low | Handler change only; no schema change | Standard deployment rollback |
| Regression | Low | Existing integration tests cover reset token flow | Run existing tests before merge |
| Backward compatibility | Low | No API change; no schema change | None required |

## Open Risks

| Risk ID | Risk | Impact | Owner | Expiry / Review date |
|---|---|---|---|---|
| R01 | Queue schema version not pinned — must verify before coding | Low | Dev | At implementation start |
