# Architecture Review

> Primary consumer: Architect, tech lead, delivery lead
> Purpose of this artifact: confirm architecture constraints, alignment, conflicts, and decisions that shape delivery
> Downstream use: architecture rules, delivery structure, readiness, governed contract decisions
> Do not duplicate: business-intake restatements or implementation task detail
> Keep this artifact evidence-based and architecture-focused.
> Record only constraints, conflicts, gaps, and decisions that affect planning, readiness, or implementation boundaries.
> Reference source artifacts rather than restating broad business scope.
> Treat the initial architecture input as high-level solution context first, then refine its implications for this initiative.
> Use this artifact to make initiative-specific architecture implications explicit enough for readiness, contracts, and handoff.
> Optional visual view: add a compact system context, container, or integration flow only when it materially improves understanding of boundaries, constraints, or impacted areas.
> Prefer embedded Mermaid in markdown when generating a new visual. Reference an existing authoritative diagram instead of duplicating it when one already exists.

## Constraints Identified

List only constraints that materially shape delivery, validation, rollout, or governed boundaries.
Use this section to turn high-level architecture context into initiative-relevant constraints.

## Existing-System Impact Summary

Keep this section focused on compatibility, regression, stability, rollout, or operational impact.
Include only impact that changes initiative-level delivery decisions.

## Optional Visual View

Add only when a compact context, container, or integration-flow view makes the initiative-specific architecture implications clearer than text alone.
Prefer one focused embedded Mermaid diagram or a short reference to an existing authoritative architecture source.

## BRS Alignment

State only alignment or misalignment that changes architecture or delivery decisions.

## Conflicts

Do not soften or hide conflicts. Make ownership and decision need explicit.

## Open Decisions

List only decisions that must be resolved or explicitly accepted for the workflow to proceed safely.
Prefer decisions that materially affect readiness, governed contracts, validation, or handoff.
