# Standalone Mode

No separate adapter is required.

If no downstream framework is used, continue with:

```text
prompts/03-openspec-handoff/
prompts/04-copilot-implementation/
prompts/05-reviewers/
```

In standalone mode, this framework remains the source of truth for engineering execution.
