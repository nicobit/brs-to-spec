# Advanced Adapter — adapter-kiro

OpenSpec is the default downstream layer.

Use this adapter only when the team intentionally chooses this downstream tool.

Keep the adapter focused on the active deliverable.
